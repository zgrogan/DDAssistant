/*package test.java;

import static org.junit.Assert.*;
import org.junit.Test;
import ddassistant.Slide;

public class SlideTest {

	@Test
	public void testSlide() {
		Slide slide = new Slide(500, 10, 0);
		Slide slide2 = new Slide(500, 10, 0);
		assertTrue(slide.equals(slide2));
		slide2.setStartDepth(450);
		assertTrue(slide2.compareTo(slide) < 0);
		assertTrue(slide.compareTo(slide2) > 0);
	}
}*/
